AUDIO FOLDER — drop your royalty-free .mp3 tracks here.

Each rendered video gets a random track from this folder. Put a few
.mp3 files in here (they must end in .mp3). If it is empty, video
processing will skip the burn (the processor refuses to render without
audio to avoid shipping silent clips).

Example names: sound_1.mp3, sound_2.mp3, ...
