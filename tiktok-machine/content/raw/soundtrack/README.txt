OPTIONAL — this folder mirrors the legacy tiktokflow_vps "soundtrack" dir.

The active audio folder is configured by config.yaml -> content.sounds_dir
(default "sounds/" at the project root). Drop your .mp3 tracks there.

This "soundtrack" folder is kept only as a familiar landing spot during
migration; the pipeline reads sounds/.
